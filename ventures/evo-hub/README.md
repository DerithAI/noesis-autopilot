# 🧬 EVO-HUB — Evolution Integration Hub

> **NOESIS + LUMEN OS + Open Design + ITDD = Living Cognitive Workspace**

EVO-HUB is an evolution integration hub that combines four systems into one dashboard:

| System | Role | Port |
|--------|------|------|
| **NOESIS** | SQLite episodic memory + autopilot cycles | — (local DB) |
| **LUMEN OS** | ChromaDB vector memory + 6-stage cognitive loop | 8002 |
| **Open Design** | Design systems + craft rules + design tokens | — (files) |
| **ITDD** | Test-first workflow (red → green → refactor) | — (tests) |

---

## 🚀 Quick Start

### 1. Start LUMEN OS
```bash
cd C:\Users\Main\SELF-EVOLVING-SYSTEM\m-ai-self
uvicorn apps.api.main:app --port 8002
```

### 2. Start EVO-HUB Backend
```bash
cd ventures/evo-hub/backend
uvicorn main:app --port 8000
```

### 3. Start EVO-HUB Frontend
```bash
cd ventures/evo-hub/frontend
npm install
npm start
```

### 4. Run ITDD Tests
```bash
cd ventures/evo-hub
python -m pytest tests/test_itdd.py -v
```

---

## 🧠 Cognitive Loop

```
[Perception] → [Intention] → [Context] → [Reasoning] → [Response] → [Direction]
```

Run manually:
```bash
cd ventures/evo-hub
python agents/evo_agent.py loop "Generate a crypto dashboard"
```

---

## 🎨 Design System

Uses Open Design craft rules:
- `frontend/DESIGN.md` — tokens, colors, typography, animation
- References: `C:\PROJECT\MAIN\open-DESIGN\craft\`

Key principles:
- Accessibility first (contrast ≥ 4.5:1)
- Animation: 150–300ms, ease-out
- Touch targets: ≥44×44px
- State coverage: default/hover/active/disabled/loading/error/success

---

## 🧪 ITDD Workflow

Every feature:
1. **Red** — `python agents/itdd_agent.py red <feature>`
2. **Green** — Write minimal code
3. **Refactor** — Clean up while tests pass
4. **Commit** — Clear WHY message

Check compliance:
```bash
python agents/itdd_agent.py check
```

---

## 📁 Structure

```
evo-hub/
├── backend/          # FastAPI + LUMEN bridge
│   └── main.py
├── frontend/         # React + Open Design craft
│   ├── DESIGN.md
│   └── src/
├── memory/           # Memory bridges
│   └── lumen_bridge.py
├── agents/           # Cognitive + ITDD agents
│   ├── evo_agent.py
│   └── itdd_agent.py
├── tests/            # ITDD red specs
│   └── test_itdd.py
└── README.md
```

---

## 🔗 External Connections

| System | Path | Junction |
|--------|------|----------|
| LUMEN OS | `C:\Users\Main\SELF-EVOLVING-SYSTEM` | Local |
| Open Design | `C:\PROJECT\MAIN\open-DESIGN` | Local |
| ITDD Rules | `F:\MANUS_PROJECT\...\ITDD.md` | Hardcoded |

---

*Generated by Venture-Swarm v1.0 + Manual Integration*
*EVO-FORGE v4.0 | LUMEN OS Stage 3 | Open Design 0.10.0 | ITDD*
